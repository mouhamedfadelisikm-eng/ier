import{Component,OnInit,inject,signal}from'@angular/core';import{CommonModule}from'@angular/common';import{FormsModule}from'@angular/forms';import{HttpErrorResponse}from'@angular/common/http';import{AuthService}from'../../core/auth/auth.service';@Component({selector:'app-profil',standalone:true,imports:[CommonModule,FormsModule],template:`<div class="page"><div><h1>Mon profil</h1><p>Informations du compte administrateur connecté.</p></div>@if(success()){<div class="alert success">{{success()}}</div>}@if(error()){<div class="alert">{{error()}}</div>}<div class="card"><div class="grid"><div><label>Prénom</label><input class="control" [ngModel]="prenom()" (ngModelChange)="prenom.set($event)"></div><div><label>Nom</label><input class="control" [ngModel]="nom()" (ngModelChange)="nom.set($event)"></div><div><label>Email</label><input class="control" type="email" [ngModel]="email()" (ngModelChange)="email.set($event)"></div><div><label>Téléphone</label><input class="control" [ngModel]="telephone()" (ngModelChange)="telephone.set($event)"></div><div class="full"><label>Adresse</label><textarea class="control" rows="3" [ngModel]="adresse()" (ngModelChange)="adresse.set($event)"></textarea></div><div><label>Nouveau mot de passe</label><input class="control" type="password" [ngModel]="password()" (ngModelChange)="password.set($event)"></div><div><label>Confirmation</label><input class="control" type="password" [ngModel]="confirmation()" (ngModelChange)="confirmation.set($event)"></div></div><div class="footer"><span class="role">Rôle : {{auth.currentUser()?.role}}</span><button class="btn" (click)="save()" [disabled]="saving()">{{saving()?'Enregistrement...':'Enregistrer'}}</button></div></div></div>`,styles:[`.page{display:flex;flex-direction:column;gap:1.2rem}.page h1{font-size:1.75rem;color:var(--slate-900)}.page>div>p{color:var(--slate-500);margin-top:.3rem}.card{background:#fff;border:1px solid var(--slate-200);border-radius:var(--radius-md);padding:1.25rem}.grid{display:grid;grid-template-columns:1fr 1fr;gap:1rem}.full{grid-column:1/-1}.grid label{display:block;font-size:.82rem;font-weight:600;margin-bottom:.35rem}.control{width:100%;padding:.65rem .8rem;border:1px solid var(--slate-300);border-radius:var(--radius-sm)}.footer{display:flex;justify-content:space-between;align-items:center;margin-top:1.25rem}.role{color:var(--slate-500)}.btn{padding:.65rem 1rem;background:var(--primary-600);color:#fff;border:0;border-radius:var(--radius-sm);cursor:pointer}.btn:disabled{opacity:.6}.alert{padding:.75rem 1rem;border-radius:var(--radius-sm);background:var(--danger-50);color:var(--danger-700)}.success{background:var(--primary-50);color:var(--primary-800)}` ]})
export class ProfilComponent implements OnInit{readonly auth=inject(AuthService);prenom=signal('');nom=signal('');email=signal('');telephone=signal('');adresse=signal('');password=signal('');confirmation=signal('');saving=signal(false);error=signal<string|null>(null);success=signal<string|null>(null);ngOnInit(){const u=this.auth.currentUser();if(u){this.prenom.set(u.prenom);this.nom.set(u.nom);this.email.set(u.email);this.telephone.set(u.telephone||'');this.adresse.set(u.adresse||'')}else{this.auth.loadCurrentUser().subscribe(u=>{this.prenom.set(u.prenom);this.nom.set(u.nom);this.email.set(u.email);this.telephone.set(u.telephone||'');this.adresse.set(u.adresse||'')})}}save(){this.error.set(null);this.success.set(null);if(this.password()!==this.confirmation()){this.error.set('Les mots de passe ne correspondent pas.');return}const body:any={prenom:this.prenom(),nom:this.nom(),email:this.email(),telephone:this.telephone()||null,adresse:this.adresse()||null};if(this.password()){body.password=this.password();body.password_confirmation=this.confirmation()}this.saving.set(true);this.auth.updateProfile(body).subscribe({next:u=>{this.saving.set(false);this.password.set('');this.confirmation.set('');this.success.set('Profil mis à jour.');},error:(e:HttpErrorResponse)=>{this.saving.set(false);this.error.set(e.error?.message||'Impossible de mettre à jour le profil.')}})}}
