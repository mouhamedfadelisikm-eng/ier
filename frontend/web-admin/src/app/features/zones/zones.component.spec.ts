import {ComponentFixture,TestBed} from '@angular/core/testing';import{provideHttpClient}from'@angular/common/http';import{HttpTestingController,provideHttpClientTesting}from'@angular/common/http/testing';import{ReferenceService}from'../../core/services/reference.service';import{ZonesComponent}from'./zones.component';describe('ZonesComponent',()=>{let f:ComponentFixture<ZonesComponent>;let c:ZonesComponent;let h:HttpTestingController;beforeEach(async()=>{await TestBed.configureTestingModule({imports:[ZonesComponent],providers:[ReferenceService,provideHttpClient(),provideHttpClientTesting()]}).compileComponents();f=TestBed.createComponent(ZonesComponent);c=f.componentInstance;h=TestBed.inject(HttpTestingController)});afterEach(()=>h.verify());it('charge les zones',()=>{f.detectChanges();h.expectOne(x=>x.url.includes('/zones')).flush({data:[{id:1,nom_zone:'Plateau'}],meta:{current_page:1,last_page:1}});expect(c.zones().length).toBe(1)});it('crée une zone',()=>{c.openCreate();c.name.set('Mermoz');c.save();h.expectOne(x=>x.url.endsWith('/zones')).flush({data:{id:2,nom_zone:'Mermoz'}});expect(c.successMessage()).toBe('Zone créée.')})});
