import{ComponentFixture,TestBed}from'@angular/core/testing';import{provideHttpClient}from'@angular/common/http';import{HttpTestingController,provideHttpClientTesting}from'@angular/common/http/testing';import{ReferenceService}from'../../core/services/reference.service';import{TypesDechetsComponent}from'./types-dechets.component';describe('TypesDechetsComponent',()=>{let f:ComponentFixture<TypesDechetsComponent>;let c:TypesDechetsComponent;let h:HttpTestingController;beforeEach(async()=>{await TestBed.configureTestingModule({imports:[TypesDechetsComponent],providers:[ReferenceService,provideHttpClient(),provideHttpClientTesting()]}).compileComponents();f=TestBed.createComponent(TypesDechetsComponent);c=f.componentInstance;h=TestBed.inject(HttpTestingController)});afterEach(()=>h.verify());it('charge les types',()=>{f.detectChanges();h.expectOne(x=>x.url.includes('/types-dechets')).flush({data:[{id:1,libelle:'Plastique'}]});expect(c.types().length).toBe(1)});it('refuse un libellé vide',()=>{c.openCreate();c.save();expect(c.formError()).toBe('Le libellé est obligatoire.');})});
