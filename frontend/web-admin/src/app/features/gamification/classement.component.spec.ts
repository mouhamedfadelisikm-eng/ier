import{ComponentFixture,TestBed}from'@angular/core/testing';import{provideHttpClient}from'@angular/common/http';import{HttpTestingController,provideHttpClientTesting}from'@angular/common/http/testing';import{provideRouter}from'@angular/router';import{GamificationService}from'../../core/services/gamification.service';import{ClassementComponent}from'./classement.component';describe('ClassementComponent',()=>{let f:ComponentFixture<ClassementComponent>;let h:HttpTestingController;beforeEach(async()=>{await TestBed.configureTestingModule({imports:[ClassementComponent],providers:[GamificationService,provideHttpClient(),provideHttpClientTesting(),provideRouter([])]}).compileComponents();f=TestBed.createComponent(ClassementComponent);h=TestBed.inject(HttpTestingController)});afterEach(()=>h.verify());it('charge le classement',()=>{f.detectChanges();h.expectOne(x=>x.url.includes('/gamification/leaderboard')).flush({data:[{rank:1,user:{id:1,nom:'Doe',prenom:'Jane'},total_points:50}],meta:{limit:100,count:1}});expect(f.componentInstance.rows().length).toBe(1);expect(f.componentInstance.loading()).toBe(false)})});