import{ComponentFixture,TestBed}from'@angular/core/testing';import{provideHttpClient}from'@angular/common/http';import{HttpTestingController,provideHttpClientTesting}from'@angular/common/http/testing';import{provideRouter}from'@angular/router';import{GamificationService}from'../../core/services/gamification.service';import{HistoriquePointsComponent}from'./historique-points.component';describe('HistoriquePointsComponent',()=>{let f:ComponentFixture<HistoriquePointsComponent>;let h:HttpTestingController;beforeEach(async()=>{await TestBed.configureTestingModule({imports:[HistoriquePointsComponent],providers:[GamificationService,provideHttpClient(),provideHttpClientTesting(),provideRouter([])]}).compileComponents();f=TestBed.createComponent(HistoriquePointsComponent);h=TestBed.inject(HttpTestingController)});afterEach(()=>h.verify());it('charge l’historique',()=>{f.detectChanges();h.expectOne(x=>x.url.includes('/historique-points')).flush({data:[{id:1,nombre_points:25,motif:'Clôture',date_attribution:'2026-09-24'}],meta:{current_page:1,last_page:1,total:1}});expect(f.componentInstance.items().length).toBe(1);expect(f.componentInstance.loading()).toBe(false)})});