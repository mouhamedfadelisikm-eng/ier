import{ComponentFixture,TestBed}from'@angular/core/testing';import{provideHttpClient}from'@angular/common/http';import{HttpTestingController,provideHttpClientTesting}from'@angular/common/http/testing';import{provideRouter}from'@angular/router';import{UserAdminService}from'../../../core/services/user-admin.service';import{UtilisateursListComponent}from'./utilisateurs-list.component';describe('UtilisateursListComponent',()=>{let f:ComponentFixture<UtilisateursListComponent>;let c:UtilisateursListComponent;let h:HttpTestingController;beforeEach(async()=>{await TestBed.configureTestingModule({imports:[UtilisateursListComponent],providers:[UserAdminService,provideHttpClient(),provideHttpClientTesting(),provideRouter([])]}).compileComponents();f=TestBed.createComponent(UtilisateursListComponent);c=f.componentInstance;h=TestBed.inject(HttpTestingController)});afterEach(()=>h.verify());it('charge les utilisateurs',()=>{f.detectChanges();h.expectOne(x=>x.url.includes('/users')).flush({data:[{id:1,nom:'Doe',prenom:'Jane',email:'j@x.test',role:'agent'}],meta:{current_page:1,last_page:1}});expect(c.users().length).toBe(1)});it('valide le mot de passe',()=>{c.openCreate();c.nom.set('Doe');c.prenom.set('Jane');c.email.set('j@x.test');c.password.set('password1');c.passwordConfirmation.set('different');c.create();expect(c.formError()).toBe('Les mots de passe ne correspondent pas.')})});
