name: CI Probe

on:
  push:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: ci-probe-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  probe:
    name: Probe
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7.0.1
      - run: echo "probe ok"
